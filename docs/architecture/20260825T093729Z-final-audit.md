# Microharness migration final audit architecture

`fx-win` is the provider side of the split: it builds the Windows executable and exposes the fx-specific adapter/bridge contract. `microharness` is the consumer-side replay and evaluation project. Specs in microharness reference the sibling binary explicitly, so the two ownership areas remain separate without copying implementation.
