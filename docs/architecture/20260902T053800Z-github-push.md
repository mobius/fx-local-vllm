# fx-win 合并提交推送架构记录

```text
origin/main 004afb6
       |
       | ordinary fast-forward push
       v
local main edbf2b51
       |
       v
GitHub fx-local-vllm main
```

本轮只推进用户 fork 的 `main` 引用，不修改 `upstream`，不创建强制历史替换。
