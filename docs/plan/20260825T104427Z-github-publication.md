# GitHub publication plan

1. Record the current fx-win release scope.
2. Build the Windows binary and run the focused adapter/bridge tests.
3. Stage the current fx-win implementation, adapter, fixtures, and docs without touching unrelated existing changes.
4. Create a descriptive commit on `main` and push it to `origin/main`.
5. Verify the remote commit and clean working tree.
