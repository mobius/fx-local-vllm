# GitHub publication research

- Current repository: `D:\Github\temp\fx-win`.
- Current remote: `https://github.com/mobius/fx-local-vllm.git`.
- Target branch: the existing `main` branch and its `origin/main` tracking branch.
- The working tree contains the Windows/Zig 0.16 compatibility work, Windows adapter/bridge fixtures, and fx-only documentation pointers.
- Credential scanning found only synthetic AWS-key-shaped test literals already used by masking tests; no real credential was added.
